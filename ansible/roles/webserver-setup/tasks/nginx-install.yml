---
- name: Install Nginx
  become: true
  ansible.builtin.apt:
    name: nginx
    state: present
    update_cache: true

- name: Ensure Nginx is started and enabled
  become: true
  ansible.builtin.service:
    name: nginx
    state: started
    enabled: true
