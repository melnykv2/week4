# deploy

Updates the Django application from GitHub, installs Python dependencies, retrieves secrets from SSM Parameter Store, writes the application environment, runs migrations/static collection, and starts Gunicorn.
